/**
 * Prototype switch for authentication forms and verification.
 *
 * Set this to `true` when the real form validation and email/phone
 * verification flow should be enforced.
 */
export const FORM_HANDLING_AND_VERIFICATION_ENABLED = false;
